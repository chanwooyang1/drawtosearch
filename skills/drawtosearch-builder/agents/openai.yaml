interface:
  display_name: "DrawToSearch Builder"
  short_description: "Build and iterate on the DrawToSearch service"
  default_prompt: "Implement or refine the DrawToSearch mobile-first sketch-to-search web app using the repo defaults."
